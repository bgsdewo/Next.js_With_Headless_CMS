export default function Layout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>LearnNextJs</title>
      </head>
      <body>{children}</body>
    </html>
  );
}
